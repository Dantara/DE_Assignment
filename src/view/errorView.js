import RootView from './rootView.js';

export default class ErrorView extends RootView{
    constructor(euler, impovedEuler, rungeKutta){
        super(null, euler, impovedEuler, rungeKutta);
    }
}
